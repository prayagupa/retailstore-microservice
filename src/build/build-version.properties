project.artifactId=${project.artifactId}
project.version=${project.version}
build.dateTime=${ci.build.dateTime}
package_name=${project.artifactId}-${project.version}-${ci.build.dateTime}.${project.packaging}