apiVersion: v1
kind: Service
metadata:
  name: rest-server
  labels:
    app: rest-server
    tier: backend
spec:
  type: NodePort
  ports:
    # the port that this service should serve on
  - port: 8080
  selector:
    app: rest-server
    tier: backend
